You are implementing Phase 17 of a phased performance-optimization effort on the MOHID Land overland-flow solver, in the repo Mohid. Target file: Software/MOHIDLand/ModuleRunOff.F90 (~19k lines). This is a HIGH-RISK phase touching the DynamicWave hot path - proceed carefully, prove correctness before optimizing, and validate with the in-run A/B harness.

READ FIRST (authoritative, in order):
1. Repo memory /memories/repo/mohid-fortran-perf.md - build/edit gotchas + EVERY prior-phase lesson. In particular: the "IEEE comparison wrappers" note, the "Build gotchas" + "NO VS2017 ON THIS MACHINE" notes, the "In-run A/B profiling harness" note, Phase 4 (unrolled face dispatch), Phase 6 (NUM_THREADS(RunOffBoxThreads(...)) gating), Phase 10 (merged X/Y SetMatrixValue), Phase 11 (cached OverLandCoefficient**2, and the PARKED cube-root HydraulicRadius**(4./3.) landmine), Phase 15 (why scan-based rebuilds lose), and the measurement-noise lesson (this machine has +/-15-30% run-to-run wall noise -> trust the in-run A/B harness, NOT single before/after wall times).
2. The living plan MOHID_ModuleRunOff_WorkSet_Optimization_Plan.md - the Phase 17 section, correction note #2 (Cluster A premise), and the Ordering rationale (17 and 18 are the two DynamicWave landmines; NEVER in flight simultaneously).
3. MOHID_Performance_Optimization_Plan.md - the A/B harness methodology + validation conventions.
4. BranchAnalysis_ExperimentalBranches.md - the analysis of the source branch MergeVelocityAdvection_NoWC_PreLoadVel.

WHITESPACE / EDITING DISCIPLINE (MANDATORY - a prior phase got this wrong):
- Do NOT strip trailing whitespace, reformat, or re-indent any line you are not functionally changing. Disable any format-on-save / trim-trailing-whitespace behavior. The repo files are CRLF (core.autocrlf=true) and many lines carry trailing whitespace; a file-wide whitespace change produces thousands of spurious diff lines and breaks review. Make ONLY surgical edits. After building, run `git diff -w --stat` - if it reports far more than your real change, you accidentally touched whitespace; fix it before committing.
- After any devenv build, run `git status` / `git diff` and `git checkout --` any incidental `.vfproj` churn devenv introduces in projects you did not intend to change (known gotcha).

BRANCH: cut perf/Phase17 from perf/Phase16 (current tip, commit 25b30df3). Do NOT start Phase 18 in this branch.

------------------------------------------------------------
BACKGROUND: what actually duplicates work (corrected premise)
------------------------------------------------------------
The hot path is HydrodynamicApproximation=DynamicWave_, GridIsConstant=.true., LimitToCriticalFlow=.true. -> DynamicWaveXX_default_CG / DynamicWaveYY_default_CG (DynamicWaveXX/YY are thin dispatchers that select the _default_CG variant). Per sub-iteration, ComputeFaceVelocityModulus sweeps CurrentWorkSize computing face velocity moduli, and THEN DynamicWaveXX/YY_default_CG each sweep CurrentWorkSize again, re-reading FlowXOld/YOld + AreaU/AreaV and recomputing face velocities to build advection. NOTE (verified, do not "fix" as a bug): XX and YY do NOT compute the same advection twice - XX builds X-face advection from FlowXOld/AreaU, YY builds Y-face advection from FlowYOld/AreaV. The real waste P17 targets is the REPEATED stencil traversal + recomputation of face velocities shared between ComputeFaceVelocityModulus and the two DynamicWave passes.

------------------------------------------------------------
THE CHANGE (port from MergeVelocityAdvection_NoWC_PreLoadVel, reconciled to the CURRENT code)
------------------------------------------------------------
Goal: compute the shared per-face velocity/advection quantities ONCE and have DynamicWaveXX/YY_default_CG consume the cached values instead of re-traversing the stencil; additionally preload neighbor face velocities (flow/area) into locals/registers within the fused loop.
- Extract the shared computation (the experimental branch calls it ComputeFaceVelocityModulus_SemWaterColumn_Vel and caches AdvectionTermU/V). Reconcile it with the CURRENT code, which already has: Phase 4 unrolled East/North face blocks, Phase 6 NUM_THREADS(RunOffBoxThreads(Me%CurrentWorkSize)) on the OMP regions, Phase 10 merged X/Y SetMatrixValue, Phase 11 cached Me%OverLandCoefficientXSquare/YSquare. The experimental branch forked from MohidLand_Bentley (pre-Phases 1-16), so DO NOT copy it verbatim - port the idea onto the current routines.
- DROP the experimental branch's broken per-timestep-allocation harness entirely (it allocates caches every timestep; if you add caches, allocate them ONCE at construction sized to Me%Size and reuse - never per-timestep).
- Preserve the CurrentWorkSize bounds + 1-cell halo semantics exactly (the DynamicWave stencil reads active +/-1). Keep the NUM_THREADS gating and the merged-region structure.
- Keep the PARKED cube-root friction term (HydraulicRadius**(4./3.)) exactly as-is - it is a separate landmine, out of scope here.

MANDATORY PRE-WORK (do this BEFORE writing any fused code, and report it):
Prove the merged advection stencil is logically identical to the CURRENT originals (the X-face advection build in DynamicWaveXX_default_CG and the Y-face build in DynamicWaveYY_default_CG). The branch analysis flagged a condition mismatch in the merged version: a face-availability test written as `ComputeFaceV(i+1,j-1) + ComputeFaceV(i+1,j) > 0` versus the original `... == 2`. These are NOT equivalent (OR vs AND). Determine what the CURRENT originals actually require, and make the fused version match them EXACTLY. If you cannot make the fused stencil bit-for-bit equivalent to the originals, STOP and report - do not ship a silent trajectory change. Enumerate every array the fused routine reads and writes (at minimum: lFlowX/lFlowY, iFlowX/iFlowY, FlowXOld/FlowYOld, AreaU/AreaV, myWaterVolume, myWaterVolumePred, ActivePoints, ActivePoints_Left, OpenPoints, any face-velocity/advection caches) and confirm the fusion does not change read/write order in a way that alters results.

------------------------------------------------------------
VALIDATION (this is a Phase-1 OpenPoints cell-flip landmine - NoRain-sensitive)
------------------------------------------------------------
IN-RUN A/B HARNESS (temporary; persistent buffers allocated ONCE, NEVER per-timestep):
- Implement a dual-compute A/B for the fused path vs the original path, in ONE process (noise-cancelling). Because DynamicWaveXX/YY are read-modify-write, snapshot ALL state they mutate (enumerated above) into persistent module buffers, run the ORIGINAL routines, capture their outputs, RESTORE the snapshot, run the FUSED routines, then compare outputs via CheckProfileMatrixDiff (real(8); tol abs 1e-5, rel 1e-4) on lFlowX AND lFlowY (and also myWaterVolume / myWaterVolumePred). Stop on mismatch. Keep the FUSED result live (compute it last) so the trajectory follows the performant path only after it is proven to match that step.
  - This is the Phase 6/10/11 single-routine dual-run pattern. Confirm the fused routines are pure w.r.t. external modules (no DrainageNetwork/SWMM/discharge calls inside) so snapshot/restore of Me% arrays is sufficient - if any external-module state is touched, that state cannot be snapshotted (same class as the Phase 6 ERR010 blow-up) and the dual-run is unsafe; narrow the A/B scope accordingly.
  - Watch the NoRain OpenPoints cell-flip explicitly: also snapshot/compare OpenPoints and ActivePoints after the step; a single flipped cell is the Phase-1 landmine signature.
- If the fusion is provably BIT-IDENTICAL (no reordered float ops), the harness should show ZERO diff and compare_mohid should be zero-diff - prefer this. If the fusion reorders floating-point operations (e.g. different summation order in the advection accumulation), it is NOT bit-identical; in that case the harness enforces the tolerance AND you MUST additionally run a LONG-RUN bounded-drift study on BOTH scenarios (confirm the diff stays bounded, does not accumulate) before accepting - NoRain is the sensitive scenario. State clearly which case applies.
- Remove ALL harness scaffolding after confirmation, before committing.

FULL-PROCESS compare_mohid (both scenarios, authoritative):
- Build Release Double OpenMP|x64 via the VS2022 console devenv (no VS2017 on this machine - see repo memory for the exact devenv.com command and the devenv.exe/MSBuild failure modes). /MP means per-file compiler overrides are ignored.
- Run both models (CWD = each exe\ folder; sync one-liner: cd "..."; .\MOHIDLand.exe):
    WithRain: D:\Work\Projects\Performance2D\Benchmarks\UK_EPA_Benchmark_Test8A\Performance_LargeModel_quickversion\exe\
    NoRain:   ...\Performance_LargeModel_quickversion_noRain\exe\
- Validate: python compare_mohid.py <res dir>  (the sibling ...\res\ one level UP from exe\, NOT exe\res\). Accepted known FAIL: FloodPeriod.dat. Expect no new failures both scenarios.

PERFORMANCE MEASUREMENT:
- Use the ModuleStopWatch per-label table (set OUTWATCH in Nomfich.dat) for ComputeFaceVelocityModulus + DynamicWaveXX_default_CG + DynamicWaveYY_default_CG CPU/Wall, comparing fused vs original. Do NOT trust a single before/after wall-clock delta (machine noise +/-15-30%); rely on the in-run A/B StopWatch labels (Baseline vs Perf in one process) or many repeated samples. Reference NoRain wall costs (10T, per-routine Wall inside ModifyRunOff, from repo memory CLEAN HOT LIST): ComputeFaceVelocityModulus ~12.3s, DynamicWaveXX_default_CG ~20.4s, DynamicWaveYY_default_CG ~19.2s - the fusion should reduce the combined face-velocity recomputation.

COMMIT / DROP RULE:
- Commit on perf/Phase17 only if: (a) the A/B harness confirms match (zero-diff if bit-identical, or within-tolerance + bounded long-run drift if not), (b) compare_mohid has no new failures on BOTH scenarios, and (c) there is a measured (A/B, noise-cancelled) improvement. If the fusion cannot be made correct, or only breaks even, DROP it and record the negative result (like Phase 15) rather than forcing it.

PLAN + MEMORY UPDATES (before reporting back):
- Update the Phase 17 section in the plan (mark DONE or documented-drop; record the stencil-equivalence finding, bit-identical-or-drift verdict, A/B + compare_mohid results, measured deltas) and add a Files Modified row. Update /memories/repo/mohid-fortran-perf.md with a concise Phase-17 result note (avoid the known str_replace corruption trigger - a regex-like literal ending in $ immediately before a backtick).

REPORT BACK:
(1) branch/commit (or drop decision + rationale);
(2) the stencil-equivalence proof (OR-vs-AND resolution) and whether the fusion is bit-identical or a bounded perturbation;
(3) exactly which routines/arrays were fused/cached and how caches are allocated once;
(4) A/B harness result (zero-diff or tolerance + long-run drift), including the NoRain OpenPoints cell-flip check;
(5) compare_mohid counts both scenarios;
(6) measured CPU/Wall deltas (A/B, noise-cancelled) for the three routines;
(7) confirmation Phase 18 (volume decoupling) was NOT started here, and the cube-root friction landmine was left untouched;
(8) the plan + memory edits.

------------------------------------------------------------
BUILD GOTCHAS TO CARRY (from repo memory)
------------------------------------------------------------
- /standard-semantics implicitly enables /assume:ieee_compares (perf trap - already fixed at config level, do NOT reintroduce).
- The DEC $OPTIONS /fpe=3 source directive is ignored by Intel Fortran; /fpe is compile-flag only.
- Async terminal calls do not preserve cd reliably - use sync one-liners (cd "..."; .\MOHIDLand.exe).
- ModuleStopWatch per-label CPU/Wall table only appears if OUTWATCH is set in Nomfich.dat.
- Working devenv build command (run from Solutions\VisualStudio2017_IntelFortran18\MOHIDNumerics\):
  & "C:\Program Files\Microsoft Visual Studio\2022\Professional\Common7\IDE\devenv.com" MOHIDNumerics.sln /Rebuild "Release Double OpenMP|x64" /Project "MOHIDLand\MOHIDLand.vfproj" > build.log 2>&1
  Check success via Select-String "error|========== (Build|Rebuild)" in build.log.
